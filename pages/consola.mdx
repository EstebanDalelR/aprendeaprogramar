import Image from "next/image";

# Consola

La consola también es llamada terminal o línea de comandos. Esta se usa para ejecutar comandos en el sistema.

## Abrir la consola

En esta ocasión, habrá diferencias entre tener un sistema Windows y uno IX (Mac o Linux), por ello debo diferenciarlos.

### Windows

#### CMD

Usa <kbd>Windows</kbd> y <kbd>R</kbd> para traer el programa <samp>Ejecutar</samp> y escribe `cmd`.

<Image
  src="/consola/windows-r-cmd.png"
  width={440}
  height={308}
  alt="Instrucciones para abrir la consola, tecla windows y R, y escribir cmd"
/>

#### Powershell

Recientemente, Microsoft creó otra consola que se acerca más a la experiencia Linux, llamada Powershell. Recomiendo esta.
La puedes encontrar en el Menú escribiendo <samp>Powershell</samp>

<Image
  src="/consola/powershell.png"
  width={805}
  height={337}
  alt="Instrucciones para abrir Powershell, menú y escribir Powershell"
/>

#### WSL

Esta es mi absoluta favorita. Windows ahora ofrece la experiencia linux completa en su sub-sistema. Para acceder a WSL, [deberás seguir las instrucciones](https://docs.microsoft.com/es-es/windows/wsl/install). Curiosamente, empiezas desde Powershell, pero ya sabes llegar ahí.

> Con WSL los comandos a usar serán los de Linux/Mac, tendrás un sistema IX

### Mac

Mac tiene por defecto su terminal, que encuentras en Spotlight.  
Usa <kbd>Command</kbd> y <kbd>Espacio</kbd> y escribe <samp>terminal</samp>.

<Image
  src="/consola/terminal.png"
  width={512}
  height={71}
  alt="Instrucciones para abrir la terminal, command, espacio y escribir terminal"
/>

### Linux

Si tienes Linux, ya sabes esto, pero dependerá de tu distribución.

## Saber dónde estoy

La consola se navega por texto y es importante entender cómo funciona esto. Para el sistema operativo, todo es un archivo, incluso las carpetas. Las abstracciones que se crean vienen después, por eso es tan útil poder ver dónde estamos.

### Windows

```
cd
```

En Windows usamos `$cd` sin parámetros para ver la posición actual. En inglés _change directory_, cambiar directorio tiene una pequeña complicación: solo aplica para el disco actual; si estás en `C:`, no podrás usarlo para ir a `D:`.

### IX

```
pwd
```

Para los IX usamos `pwd`. Este comando sólo ejecuta una función. En inglés _print working directory_, imprimir directorio de trabajo.

## Crear un archivo
## Crear una carpeta
## Mover un archivo
## Mover una carpeta
## Eliminar un archivo
## Eliinar una carpeta
## Cerrar la consola


